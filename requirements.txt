# Core dependencies
chromadb>=1.3.0
sentence-transformers>=2.5.0
langchain-community>=0.0.1
langchain-text-splitters>=0.0.1

# MCP Server
fastmcp>=2.0.0

# Data acquisition
beautifulsoup4>=4.12.0
requests>=2.31.0
gitpython>=3.1.40

# Utilities
rank-bm25>=0.2.2
redis>=5.0.0

# Cache optimizations (optional but recommended for performance)
msgpack>=1.0.0  # Faster serialization than pickle
lz4>=4.0.0  # Compression for cache data (60-80% size reduction)

# Testing
pytest>=9.0.0

# PyTorch - ROCm (for AMD GPU)
# Install separately based on your hardware:
# - ROCm: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm6.2
# - CUDA: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# - CPU only: pip install torch torchvision torchaudio

# Production enhancements
pybreaker>=1.0.0          # Circuit breaker pattern
tenacity>=8.0.0           # Retry logic with exponential backoff
apscheduler>=3.10.0       # Scheduled tasks for cache warming
prometheus-fastapi-instrumentator>=6.0.0  # Metrics instrumentation

# Week 3 enhancements - Query expansion and hybrid search
nltk==3.9.1               # Natural language processing for query expansion
spacy==3.8.2              # Advanced NLP and entity recognition
streamlit==1.39.0         # Web dashboard framework
plotly==5.24.1            # Interactive visualizations
httpx>=0.28.1             # Async HTTP client (compatible with FastMCP)
fastapi==0.115.5          # REST API framework
uvicorn[standard]==0.32.1 # ASGI server for FastAPI
slowapi==0.1.9            # Rate limiting for API
